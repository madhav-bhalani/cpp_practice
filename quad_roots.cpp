#include <iostream>
#include <cmath>
using namespace std;

int main(){

    // int a,b,c;

    // cout<<"Enter the value of a, b and c: \n";
    // cin>>a>>b>>c;

    // float r1 = (-b+sqrt(b*b - 4*a*c))/(2*a);
    // float r2 = (-b-sqrt(b*b - 4*a*c))/(2*a);

    // cout<<"The quadratic roots are: "<<r1<<" and "<<r2;

    char a = 127;
    a++;
    cout<<"value of a is "<<(int)a;

    return 0;
}